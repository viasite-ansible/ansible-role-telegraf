---
# cannot test with docker, systemd required for "Start the Telegraf service",
# "msg": "no service or tool found for: telegraf"
#driver:
#  name: docker
#docker:
#  containers:
#    - name: ansible-role-telegraf
#      image: ubuntu
#      image_version: latest
#      volume_mounts:
#        - ${MOLECULE_APTPROXY_PATH}:/etc/apt/apt.conf.d/00aptproxy
#      ansible_groups:
#        - group1

driver:
  name: vagrant
vagrant:
  platforms:
    - name: xenial64
      box: bento/ubuntu-16.04
  providers:
    - name: virtualbox
      type: virtualbox
  instances:
    - name: vagrant-01

ansible:
  playbook: tests/playbook.yml
